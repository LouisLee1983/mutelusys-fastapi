from app.localization.models import TranslationKey, TranslationValue

# 引用主模型中的TranslationKey和TranslationValue类，保持一致性
# 这样可以通过from app.localization.translation.models import TranslationKey, TranslationValue来引用
# 满足项目的模块化组织要求
